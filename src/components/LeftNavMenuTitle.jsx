import React from "react";

const LeftNavMenuTitle = ({ title }) => {
  return (
    <div className="text-white flex items-center translate-x-3 translate-y-[-5px] font-semibold">
      {title}
    </div>
  );
};

export default LeftNavMenuTitle;
